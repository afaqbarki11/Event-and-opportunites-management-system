# Server Configuration
PORT=5000

# Database Configuration (XAMPP MySQL defaults)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=eoms_db

# JWT Secret Key (change this in production)
JWT_SECRET=eoms_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d
